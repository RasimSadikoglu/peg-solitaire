#include "search.h"

int main() {
    // peg_solitaire::breadth_first_search();
    peg_solitaire::depth_first_search();
    // peg_solitaire::iterative_deepining_search();
    // peg_solitaire::depth_first_search_random_selection();
    // peg_solitaire::depth_first_search_heuristic_selection();
    // peg_solitaire::depth_limited_search(10);
}